#include <bits/stdc++.h>
using namespace std;

int main(){
  int N;
  int a, b;
  vector<vector<int>> line(N, vector<int>(2));
  for (int i; i<N; i++){
    cin >> a >> b;
    line.at(i).at(0) = a;
    line.at(i).at(1) = b;
  }



}

double middle1(double a){
  0
}